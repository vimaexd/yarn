import Discord, { Client, GuildMember, TextChannel } from "discord.js";
import Jimp from "jimp";
import { YarnGlobals } from "../utils/types";

export default async (client: Client, u: GuildMember) => {
}